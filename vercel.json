{
  "buildCommand": "cd smartplan-track && npm install && npm run build",
  "outputDirectory": "smartplan-track/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
